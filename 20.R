# Empty plot with default settings
plot.new()
title("Empty Plot")

# Empty plot with specified axis limits
plot(NA, xlim = c(0, 10), ylim = c(0, 10), xlab = "X-Axis", ylab = "Y-Axis", main = "Empty Plot with Limits")
